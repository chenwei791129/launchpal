## 1. Backend data model and codec

- [x] 1.1 Add `KeepAliveConfig` struct (`Enabled`, `Mode`, `SuccessfulExit`, `Crashed`, `AfterInitialDemand`, `NetworkState`, `PathState`, `OtherJobEnabled` with the JSON tags from design.md) in new file `internal/launchctl/keepalive.go`. Verification: `go build ./...` compiles and `go vet` passes.
- [x] 1.2 Replace `Service.KeepAlive bool` and `ServiceConfig.KeepAlive bool` with `KeepAliveConfig`, and add `ThrottleInterval *int` to both `Service` and `ServiceConfig` in `internal/launchctl/types.go`. Verification: package compiles; existing references updated to the new type.
- [x] 1.3 Rewrite `parseKeepAlive(v any) KeepAliveConfig` to follow the parse contract (nil→disabled, bool→boolean mode, map→dictionary mode with every recognized sub-key incl. NetworkState/PathState/OtherJobEnabled preserved; unknown inner values ignored, never error). Satisfies Requirement "Get service details from plist" (KeepAlive parsing). Verification: table-driven unit test in `internal/launchctl/keepalive_test.go` covering nil, bool true/false, and a full dictionary.
- [x] 1.4 Extend `BuildPlistDict` in `internal/launchctl/plist_encode.go` to emit `KeepAlive` per the encode contract (omit when disabled; `true` for boolean mode; dictionary with only non-nil bool sub-keys and non-empty maps for dictionary mode) and to emit `ThrottleInterval` only when non-nil. Satisfies Requirement "Write plist from ServiceConfig". Verification: unit test asserting each emitted/omitted key for boolean, dictionary, and ThrottleInterval set/unset cases.
- [x] 1.5 Add a `ThrottleInterval *int` field with the `plist:"ThrottleInterval"` tag to the plist-decode struct(s) (`plistData` in `internal/launchctl/user.go` and the corresponding decode path in `internal/launchctl/readonly.go`), then wire it so `Service.ThrottleInterval` is populated wherever `parseKeepAlive` is already called, completing Requirement "Get service details from plist" (ThrottleInterval). Without the struct field the plist key is never unmarshaled and the value is always nil. Verification: round-trip unit test reading a plist with KeepAlive dict + ThrottleInterval, asserting `Service.ThrottleInterval` is non-nil and equals the plist value, then writing via `BuildPlistDict` and confirming the plist keys match.
- [x] 1.6 Update existing Go tests that assume `KeepAlive` is a bool (e.g. `if !service.KeepAlive` assertions in `internal/launchctl/user_test.go`) to the `KeepAliveConfig` shape (assert `service.KeepAlive.Enabled`), and augment the existing `plist_encode` tests so the new KeepAlive/ThrottleInterval encoding paths are actually exercised rather than passing trivially. Verification: `go test ./internal/launchctl/...` compiles and passes.

## 2. Wails binding and frontend types

- [x] 2.1 [P] Update both frontend type surfaces so `keepAlive` is the object shape and `throttleInterval` is present: the hand-maintained `frontend/app/types/wails.d.ts` AND the Wails-generated `frontend/wailsjs/go/models.ts` (regenerate via `wails generate`, or edit it to match if regeneration is not run). Leaving `models.ts` as `keepAlive: boolean` leaves the runtime binding types stale. Verification: `cd frontend && npx tsc --noEmit` (or `make test` typecheck step) passes with both files updated.
- [x] 2.2 [P] Update `frontend/app/utils/serviceToConfig.ts` to map the structured `keepAlive` object and `throttleInterval` between Service and ServiceConfig shapes. Verification: existing unit tests for serviceToConfig updated and passing under `make test`.

## 3. Frontend launch-policy radio and advanced options (create modal)

- [x] 3.1 Replace the two `runAtLoad` / `keepAlive` checkboxes in `frontend/app/components/CreateServiceModal.vue` with a single "Launch Policy" radio group (`On Demand` / `Run at Load` / `Keep Alive`), and map the selection to config on submit per the spec table. Satisfies Requirement "Launch policy selection in service creation form". All strings in English. Verification: component test asserting the three radio options render and exactly one is selectable.
- [x] 3.2 Add the advanced KeepAlive section in `CreateServiceModal.vue` shown only when `Keep Alive` is selected: boolean/dictionary toggle, editable `SuccessfulExit` / `Crashed` / `AfterInitialDemand` controls, an integer `ThrottleInterval` control, and informational text for OR semantics + "implies Run at Load". When the dictionary form has no effective sub-keys, downgrade to boolean mode (matches the backend empty-dict rule). Satisfies Requirement "KeepAlive advanced options". English only. Verification: component test asserting the section appears under `Keep Alive` and is hidden otherwise, and that editing a sub-key produces a dictionary KeepAlive on submit.
- [x] 3.3 Ensure non-editable sub-keys (`NetworkState`, `PathState`, `OtherJobEnabled`) read from an existing service are carried through the form state and re-submitted unchanged. Verification: component test that loads a service with a `PathState` entry, edits only `SuccessfulExit`, and asserts `PathState` is preserved in the submitted config.

## 4. Frontend edit page round-trip (`pages/services/[name].vue`)

- [x] 4.1 Replace the dual `runAtLoad` / `keepAlive` checkboxes in the edit form of `frontend/app/pages/services/[name].vue` with the same "Launch Policy" radio group + advanced KeepAlive section used in the create modal (extract a shared component or composable to avoid duplicating the radio/advanced logic). Map the editForm state to/from `KeepAliveConfig` + `throttleInterval`. Satisfies Requirement "Launch policy selection in service creation form" and "KeepAlive advanced options" on the edit surface. English only. Verification: component/page test asserting the radio + advanced section render in edit mode and submit the correct config.
- [x] 4.2 Fix the edit-form data flow in `[name].vue` so `populateEditForm` maps an incoming `KeepAliveConfig` (incl. `ThrottleInterval` and non-editable `NetworkState`/`PathState`/`OtherJobEnabled`) into form state, and `handleSave` builds the `ServiceConfig` with the structured `keepAlive` object instead of a boolean. Satisfies the "Non-editable sub-keys are preserved through edit" scenario. Verification: page test loading a service whose `KeepAlive` dict has a `PathState` entry, editing only `SuccessfulExit`, and asserting the submitted config preserves `PathState` and `ThrottleInterval`.
- [x] 4.3 Map the incoming service to the launch-policy radio on load using the design's load-direction rule (`KeepAlive.Enabled` → `keepAlive` regardless of `RunAtLoad`; else `RunAtLoad` → `runAtLoad`; else `onDemand`), applied in both the create-modal prefill (clone) and the edit page. Verification: test that a legacy service with both `RunAtLoad=true` and a `KeepAlive` dict lands on the `Keep Alive` radio and, on save, does not emit a standalone `RunAtLoad` key.

## 5. Display and clone integration

- [x] 5.1 [P] Update `frontend/app/components/ServiceSummary.vue` to render the structured KeepAlive (mode + active sub-keys) and `ThrottleInterval` instead of a boolean. English only. Verification: `ServiceSummary.test.ts` updated to assert dictionary-mode rendering and passing.
- [x] 5.2 [P] Update `frontend/app/components/ServiceRow.vue` so the launch-policy badge reflects KeepAlive services: a service with `keepAlive.enabled` shows a launch-policy badge (it implies Run at Load) rather than appearing as on-demand. English only. Verification: component test asserting a KeepAlive-mode service renders a launch-policy badge in the row.
- [x] 5.3 Update clone behavior so the prefilled form maps the source launch policy to the radio (per the load-direction rule in 4.3) and carries `KeepAlive` advanced sub-keys + `ThrottleInterval`. Update the `CloneUserService.test.ts` / `CreateServiceModal.test.ts` fixtures (`keepAlive` now an object, not `true`) and assertions accordingly. Satisfies Requirement "Clone a user service". Verification: `CloneUserService.test.ts` and `CreateServiceModal.test.ts` updated to the object shape + radio model and passing.

## 6. Verification and docs

- [x] 6.1 Run `make test` and `make lint`; resolve any failures so both pass clean. Verification: both commands exit 0.
- [x] 6.2 Update `.claude/CLAUDE.md` Service Types / known-limitations notes to describe the structured KeepAlive + ThrottleInterval support and the launch-policy radio (create + edit). Verification: content review confirming the doc matches shipped behavior.
